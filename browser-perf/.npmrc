# Block preinstall/install/postinstall/prepare scripts for this package and every
# dependency — the mechanism behind npm supply-chain worms that execute code at
# install time. Playwright's browser download is run explicitly in the Dockerfile
# (`npx playwright install`), not via a lifecycle script, so this is safe.
ignore-scripts=true
